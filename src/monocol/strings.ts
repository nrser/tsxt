import { N_0, Optional } from "../types";
import {
  LineGenerator,
  StringGenerator,
  StringIterable,
} from "./types";

// export default function* wordWrap(
//   str: string,
//   maxWidth?: Optional<N_0>,
// ): LineGenerator {
  
// }


export function* spliterate(
  iterable: StringIterable,
  re: RegExp,
): StringGenerator {
  
}

